server {
    listen 80;
    server_name 176.123.162.249;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # CORS for GitHub Pages
    add_header Access-Control-Allow-Origin "https://nikmsk35.github.io" always;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS" always;
    add_header Access-Control-Allow-Headers "Content-Type, Authorization" always;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_buffering off;
    }

    location /api/health {
        access_log off;
        proxy_pass http://127.0.0.1:3000/api/health;
    }
}

# HTTPS (after certbot)
# server {
#     listen 443 ssl http2;
#     server_name 176.123.162.249;
#     ssl_certificate /etc/letsencrypt/live/176.123.162.249/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/176.123.162.249/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     location / { proxy_pass http://127.0.0.1:3000; ... }
# }
